import { Navbar, Termometer } from '../Components';

import './App.css';

export const App = () => {
    return (
        <div className='App'>
            <Navbar></Navbar>
            <Termometer></Termometer>
        </div>
    );
};
